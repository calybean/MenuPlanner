<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item android:id="@+id/nav_weekly_menu" android:icon="@android:drawable/ic_menu_my_calendar"
            android:title="@string/menu" />
        <item android:id="@+id/nav_inventory" android:icon="@android:drawable/ic_menu_gallery"
            android:title="@string/inventory" />
        <item android:id="@+id/nav_shopping_list" android:icon="@android:drawable/ic_menu_agenda"
            android:title="@string/shopping_list" />
    </group>

    <item android:title="Communicate">
        <menu>
            <item android:id="@+id/nav_share" android:icon="@android:drawable/ic_menu_share"
                android:title="Share" />
            <item android:id="@+id/nav_send" android:icon="@android:drawable/ic_menu_send"
                android:title="Send" />
        </menu>
    </item>

</menu>